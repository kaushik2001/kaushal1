<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appsnipp.loginsamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".Navigation_Profile.Navigation_Activity"
            android:label="@string/title_activity_navigation_"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".registration.Forgotpassword_form"></activity>
        <activity android:name=".complain.complainadd" />
        <activity android:name=".registration.Registration" />
        <activity android:name=".registration.password"></activity>
        <activity android:name=".detailtest_activity" />
        <activity android:name=".LoginActivity">

        </activity>
    </application>

</manifest>